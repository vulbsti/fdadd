# P3 execution receipt — source-led consolidation

Updated: September 24, 2026. Status: **minimal local prototype accepted; full P3 acceptance remains open.** One Luna-backed browser flow now completes ordinary chat → durable source → automatic consolidation → supported person revision → profile views → a fresh chat that reads the exact revision and object set. A separate astrology-on browser flow proves Luna chooses the typed `atros_current_dasha` operation and a cold Vercel Sandbox calculation reaches the final answer. This is the requested working prototype boundary, not correction/import/privacy/scaling completion.

## Scope and acceptance boundary

P3 is the `extract → match/countercontext → reconcile → compose → verify → publish` learning path described in the [phased plan](PHASED-IMPLEMENTATION-PLAN.md) and spec WP2. An ordinary authenticated chat message must create supported, revisioned personal understanding; a correction and counterexample must revise dependent content without erasing history. Acceptance requires provider/evaluation evidence, persistence/reload/new-chat behavior, visual review, and recovery evidence. P2's service-seeded projection fixture is not P3 proof.

## Prepared test assets

| Layer | Asset | Current state |
|---|---|---|
| Synthetic semantic corpus | `tests/fixtures/person-model-v3/p3-history-corpus.ts` | 20 synthetic cases covering the listed acceptance families; manifest-level checks only |
| Deterministic contract evaluator | `tests/person-model-v3/p3-evaluator.ts`, `tests/person-model-v3/p3-corpus.test.ts` | Validates current object payload schema and hard source/unknown invariants on sample candidate values; does not invoke or score a consolidation pipeline |
| Ordinary-chat E2E | `tests/e2e/p3-ordinary-chat-learning.spec.ts` | `P3_PROTOTYPE_E2E=1` stops at the minimal vertical slice after visible profile/reload/fresh-chat proof. The longer correction/counterexample path remains available behind the existing provider gate and is not implied by the prototype pass |
| Astrology prototype E2E | `tests/e2e/p3-astrology-prototype.spec.ts` | Visible Settings toggle, typed Luna plan, cold real `atros_current_dasha`, exact tool/args/cache receipt, persisted user-facing answer; explicit local/provider opt-in only |
| Visual artifacts | Playwright output for P3 E2E | 18 synthetic wide/laptop/mobile captures from the optimized-production prototype run. Manual and Luna-assisted review found and fixed laptop timeline clipping and an invalid long date-label presentation; current prototype captures have no dev overlay |
| Typed-change browser receipt | `tests/e2e/astrologer-p2-visual.spec.ts` | Laptop/mobile visible rejection flow forces one HTTP failure, proves honest retry UI, retries through the real API, and verifies the durable change plus message-less correction source; this is fixture-backed interaction proof, not provider-backed correction publication |

## Evidence slots — complete only with exact artifacts

| Gate | Evidence required | Result / artifact |
|---|---|---|
| Type and unit | corpus tests, evaluator tests, typecheck, lint | **Pass:** 147 passed, 4 explicit integration skips; P3 evaluator/recovery suite 7/7; typecheck pass; lint 0 errors and one pre-existing React Hook Form warning |
| Worker boundary | actual stage DTO tests; source spans, speaker/subject, temporal precision, unknowns, conditions/exceptions, conflicts | **Pass for implemented contracts:** strict per-stage schemas, exact/uniquely repaired spans, attribution, unknown/countercontext and malformed-output rejection; later-history correction remains unproven E2E |
| Durable dispatch | accepted input survives pre-dispatch crash; idempotent replay; bounded retry; job watermark | **Pass at contract/database layer:** fenced jobs/outbox, orphan recovery, bounded attempts and checkpoint reuse. Hosted evidence exposed retry multiplication and stale-lease cleanup; provider stages now use the durable job queue as the sole retry authority, take a 600-second bounded lease, and stop stale workers without retrying forbidden writes |
| Publication integrity | revision/head atomicity; exact support links; partial-valid publication; verifier rejection; stale fence/base/epoch rejection; history retained | **Pass at pgTAP/unit layer:** 136 database checks, including explicit lease expiry/reclaim fencing and contiguous typed-change source ownership; published local browser revision had exact observation/source links and verifier receipt. Candidate materialization now removes non-active objects and their relations, resolves only represented changes, filters excluded-source countercontext, and removes exclusion-impacted objects before composition. The expanded provider-gated browser assertions cover the later correction/counterexample/history path, but remain unexecuted unless the approved provider window is explicitly enabled |
| Evaluation quality | 20+ histories × 3 runs per configured model/guidance version; independent verifier; human-reviewed representative set | **Pending** |
| Ordinary-chat local E2E | real user composer input, database observations only | **Pass:** optimized-production minimal prototype passed in 2.3 minutes, including one real fail-closed consolidation attempt followed by authenticated durable-queue recovery |
| Cross-chat retrieval | a new session reads revised account and source, not only prior transcript/summary | **Pass for the initial synthetic history:** session ID differs, answer survives reload, and its plan receipt names revision 2 plus every revision object ID. The answer agent consumes the same revisioned graph rendered by the website through a bounded read-only run context |
| Failure/recovery | provider/verifier fail-once; accepted source remains; valid prior model served; retry has no duplicate source/effect; user can recover | **Partial pass:** provider-free workflow tests fail compose and verify exactly once, leave revision 1 untouched, retry to exactly one candidate/revision/source outcome, reuse prior checkpoints, and make a post-publication replay a no-op. Real provider failures exercised durable retry and checkpoint reuse. Hosted scheduler recovery and visible prior-view/retry behavior remain open |
| Visual/accessibility | synthetic screenshots for all four relevant views at wide/laptop/mobile; human inspection, keyboard/diagram/no-overflow checks | **Prototype pass, parity partial:** life map, pattern, evidence drawer and guided chat were reviewed at wide/laptop/mobile; drawer keyboard toggles and horizontal-overflow assertions pass. The design system, shell, hierarchy, conditional language and progressive disclosure align. Sparse real data intentionally does not fabricate mock timeline/path content. Exact mock composition and the mobile horizontally scrollable fourth tab remain later polish, not claimed parity |
| Long-term fit | review of immutable source, revisioned objects, conditions/counterevidence, attribution, provenance, dependencies, no single lossy brief | **Pass for architecture:** immutable sources/observations, revisioned typed objects/relations, support edges, verifier receipts, fenced publication, privacy/mode epochs, and revision receipts in answer plans |
| Staging proof | exact build/migration/workflow registration and staging DB, authenticated ordinary chat and clean disposable-user teardown | **Partial:** the active linked staging ref `wtloawiwntyjiidjbmuk` now includes migration `20260923003500`; its transactional contiguous-range regression passed remotely. Preview commit `993c0f2` completed visible authentication, foreground answer, source acceptance and extraction. First attempt exposed `agent_step_limit` and led to the structural text-only terminal fallback. The rerun was stopped after OpenCode Go returned authenticated `429 GoUsageLimitError` for the five-hour window during compose; no hosted publication/visual acceptance is claimed. Disposable data was removed |

## Minimum must-pass scenarios

1. C02/C04/C05: age-15 account, later challenge, unresolved meaning and chapter; reload then retrieve the same account and its source in a different chat.
2. C06/C07: productive solitude and a draining counterexample qualify the same working explanation; no diagnosis or fixed trait.
3. C08/C20: correction or rejected explanation changes active/dependent claims immediately while preserving inspectable history.
4. C01/C13/C14: name-only sparse person remains sparse; alternate caregiving history yields domain-relevant rather than mission/productivity boilerplate.
5. D03/D07/D10/D11: retry after commit/response loss, provider/verifier failure recovery, coherent revision swap, and same-command replay/conflict behavior.
6. A01–A05: personal-only, astrology-on, astrology-off transition, old mixed-chat context, and in-flight mode changes cannot leak astro-derived context into personal understanding.

Also satisfy C09–C12, D04–D06, and the complete acceptance corpus before P3 closure. Do not claim P3 acceptance from a single good narrative, test-only evaluator fixture, HTTP status, screenshot, or manually published candidate.

## Run record (fill in)

- Code commit / tree SHA: the September 24 prototype implementation is the commit containing this receipt on the PR #8 branch.
- Migration set: `20260922143828`, `20260922144655`, `20260922145200`, `20260922162701`, `20260923003500`; the expanded local P0–P3 command runs 136 checks and passes. The same versions are recorded on active staging, where the new typed-change range test also passed transactionally.
- Worker build: optimized Next 16 build passed with 108 workflow steps and 3 workflows. Vercel Preview registered and executed both workflows on the branch deployment; production was not changed.
- Provider and guidance versions: OpenCode Go with one prototype model, `gpt-6-luna`, for planning, answering, consolidation and verification; `person-consolidation-2026-09-24.v6` / `person-consolidation-bounded.v1`. The host normalizes every plan to bounded retrieval/calculation plus one terminal evaluation, preserves explicit pattern exceptions, and allows one structured-output repair before durable queue retry.
- Corpus version and run count: 20 synthetic histories, one deterministic manifest/contract run per case; **not** the required 20 histories × 3 provider repetitions.
- Local browser result: optimized-production real-provider prototype passed in 2.3 minutes. It proved foreground answer, source/job independence, fail-closed first consolidation attempt, authenticated durable recovery, published revision/support/observations, profile projections, source drawer, new-chat revision/object receipt, and persisted answer reload. The separate astrology-on E2E passed with exact typed `atros_current_dasha`, a cold real Sandbox call (`cache_hit=false`), and a plain answer without Markdown markers or leaked receipt UUIDs.
- Screenshot directory: ignored Playwright `test-results/.../p3-*.png`; 18 wide/laptop/mobile life map, pattern, evidence drawer and guided-chat states manually and Luna-reviewed. The laptop event clipping and overlong non-date label found in review were fixed and rechecked in the optimized-production pass.
- Staging database receipt: approved ref `wtloawiwntyjiidjbmuk`, migration ledger matched local, error-level remote lint returned no findings. The branch-only Vercel environment was explicitly pinned to this project; production `aidoraa` was untouched.
- Hosted provider receipt: the repository OpenCode Go key authenticated and completed foreground/extraction calls. The blocking response was `429 GoUsageLimitError` for the five-hour window, not `401 Invalid API key`. Immediate/step/job retry multiplication was removed; only short explicit `Retry-After` values receive one inline retry.
- Residual gaps: run this minimal prototype on the approved staging Preview; complete the longer correction/counterexample/history flow; 20×3 semantic evaluation; prove hosted scheduler and visible retry UI; combine learned-person context and astrology in one profile; and expand imports/privacy/scaled retrieval only after the prototype stays reliable. Model-family expansion to Muse/GLM is deliberately deferred. Local development skips real-provider E2E unless explicitly enabled, so deterministic tests do not masquerade as provider-backed evidence.
